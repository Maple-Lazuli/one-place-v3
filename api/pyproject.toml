[project]
name = "api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "flask>=3.1.1",
    "psycopg2>=2.9.10",
]

[project.optional-dependencies]
dev = ["ruff"]

[dependency-groups]
dev = [
    "ruff>=0.12.2",
]
